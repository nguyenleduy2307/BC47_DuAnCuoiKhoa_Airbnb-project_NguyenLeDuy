import { RegisterAdminTemplate} from "component"

// Thêm export trước const: để file index.ts bên ngoài nhận export *
export const RegisterAdmin = () => {
  return (
    <RegisterAdminTemplate/>
  )
}
